// 13. Write a script that prompt the user to enter base and height of the triangle and calculate an area of a triangle (area = 0.5 x b x h).

let baseOfTriangle = prompt("Enter base of a triangle");
let heightOfTriangle = prompt("Enter height of a triangle");

let areaOfTriangle = 0.5*baseOfTriangle*heightOfTriangle ;

console.log("Area of Triangle =", areaOfTriangle);
